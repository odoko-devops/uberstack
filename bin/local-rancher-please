#!/bin/sh

docker run --rm -it\
           --name docker-stack \
	   -v $(pwd)/config.yml:/config.yml \
	   -v $(pwd)/state:/state \
	   -v /Users/${USER}/.docker:/odoko/.docker \
	   odoko/docker-stack local
