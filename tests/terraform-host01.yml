type: host
name: terraform01
host-provider: terraform
host-name: ${host.terraform01.terraform_host01_ip}
variables:
  instance_type: t2.small
  public_key: ${PUBLIC_KEY}
resources:
  - aws_instance.terraform_host01
outputs:
  - terraform_host01_id
  - terraform_host01_ip
